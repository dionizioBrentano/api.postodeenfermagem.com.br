# Regras universais — referência humana

Versão canônica: **1.1** (2026-09-15).

Este arquivo é a lei completa (POS, PRE, ESC, VER, EST, ERR, SEG, DAD, DEP, TST, DES, VCS, DOC, §14–16).

O `AGENTS.md` da raiz e os arquivos em `.agents/rules/` são o recorte operacional. Em conflito de redação, prevalece este documento — salvo a lista NUNCA, idêntica nos dois.

## Como completar

Cole abaixo o texto integral da versão 1.1 já aprovada na conversa de 15/09/2026. Não edite regra “só neste arquivo”. Mudança de regra = versão 1.2 comunicada.

Até a colagem, os agentes devem seguir `AGENTS.md` + `.agents/rules/` + `projeto/posto-de-enfermagem.md`.
